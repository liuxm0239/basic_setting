# set a CRAN mirror
local({r <- getOption("repos")
       r["CRAN"] <- "https://mirrors.ustc.edu.cn/CRAN" # CRAN mirror
       options(repos=r)})
options(BioC_mirror="https://mirrors.ustc.edu.cn/bioc") # Bioconductor mirror
